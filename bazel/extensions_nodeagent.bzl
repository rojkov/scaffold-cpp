# Flat list of node agent extension Bazel labels.
# Comment out a line to exclude the extension from the node agent binary.
NODEAGENT_EXTENSIONS = [
    "//src/extensions/nodeagent/handlers/echo:echo",
]
